# Backup schedule (cron expression) — default: daily at 3 AM
BACKUP_CRON_EXPRESSION=0 3 * * *

# Retention — delete backups older than this many days
BACKUP_RETENTION_DAYS=7

# --- S3-compatible storage ---
AWS_S3_BUCKET_NAME=your-backup-bucket
AWS_S3_PATH=solo-stack
AWS_ACCESS_KEY_ID=CHANGE_ME
AWS_SECRET_ACCESS_KEY=CHANGE_ME
# For non-AWS S3 (e.g. Backblaze B2, MinIO, Wasabi):
# AWS_ENDPOINT=https://s3.us-west-000.backblazeb2.com
# AWS_ENDPOINT_PROTO=https

# --- Notifications (optional) ---
# Send a POST request on backup success/failure
# NOTIFICATION_URLS=https://hooks.slack.com/services/xxx/yyy/zzz

# --- Encryption (optional) ---
# GPG_PASSPHRASE=CHANGE_ME
